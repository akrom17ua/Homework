# Create a class called Person it should have 2 attributes called name and steps_today
# And it should have 2 methods called add_steps and goal_reached

class Person:

    def __init__(self, name, steps_today):
        pass
        # Continue with your own solution code

    def add_steps(self, count):
        # Here with this count parameter you need to add this count to steps_today attribute

    def goal_reached(self, goal):
        # Here goal parameter is passed it is number of steps set as goal and you need to check goal with steps_todat if it is less then goal return False else return True
