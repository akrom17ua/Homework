Create a class called Student.

The class should have three attributes:

name
age
grade

def __init__(self, name, age, grade):
   self.name = name
   #continue

Add three methods:

display_info(self)
→ prints the student's full information in a friendly sentence
Example: "Name: Ali, Age: 14, Grade: 8th"

promote(self, new_grade)
→ increases the student’s grade by one level and print student's new grade

change_name(new_name)
→ updates the student's name -> self.name = new_name and print it
